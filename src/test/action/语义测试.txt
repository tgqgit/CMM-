{
        int a[5] = {1;2;3;4;5;6};   //越界
        a[0] = 1.0;     //类型不匹配
        int w2 = 2.0;   //类型不匹配
        e = 0;          //未定义变量
        int w;
        a[1] = w;       //使用了未赋值变量
        int b[5] = {1.0;2.2;3.3};   //类型不匹配
        read(a[5]);     //数组下标越界
        ====            //错误语句
        int c[0] = {1;2;3};     //数组大小错误

        real b[3] = {1;2;3};    //变量重复定义
        real aaa = 1.4/ -0.0 + 2.4; //除0操作
        if(a<1)     //缺少数组下标
        {
            a[2] = 6;   //可以调用上一深度的变量
            b = 0.0;    //调用上一深度的数组变量时缺少下标，且数据类型不匹配
            int d;
        }
        while(a[1] < 1)
        {
            d = 0;      //无法调用if语句块中的变量
        }
}